tablet Big { x0: i64 }

fn make_big(seed: i64) -> Big {
  Big { x0: seed + 0 }
}

fn main() -> i32 {
  mut v: dvec<Big>
  mut i: i64 = 0
  while i < 200000 {
    v.push(make_big(i))
    i = i + 1
  }
  println(v.len)
  println(v[0].x0)
  println(v[199999].x0)
  0
}
